apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-pinger
  labels:
    app: pingpong-pinger
    serviceName: PingPong.Pinger
    releaseId: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong-pinger
  template:
    metadata:
      labels:
        app: pingpong-pinger
        serviceName: PingPong.Pinger
        releaseId: dev
    spec:
      containers:
      - name: pingpong-pinger
        image: pingpong:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
          protocol: TCP
        env:
        - name: INSTANCE_ID
          value: "pinger"
        - name: SIBLING_URL
          value: "http://pingpong-ponger:8080"
        # - name: HTTP_PORT
        #   value: "8080"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: pingpong-pinger
  labels:
    app: pingpong-pinger
spec:
  selector:
    app: pingpong-pinger
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pingpong-pinger-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: pingpong-pinger.localhost
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: pingpong-pinger
            port:
              number: 8080
